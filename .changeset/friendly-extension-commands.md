---
"@jmfederico/pi-web": patch
---

Improve Web UI slash-command compatibility so extension-provided commands appear in completion, run consistently with Pi runtime command handling, and can display extension-owned overlay dialogs. Align Pi runtime dependencies so peer-based extension commands such as `/btw` load correctly.

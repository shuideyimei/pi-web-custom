---
"@jmfederico/pi-web": patch
---

Refactor the composer bar into a minimalist single-line layout: the model selector now sits on the left as muted text with a hover chevron, the center uses a clean auto-growing input, and the right side is reduced to an attach icon plus a dynamic send/stop action button. Inline `@` and `/` completions now render a floating menu at the cursor. Token and cost stats moved out of the composer line into a faint, hover- and pulse-revealing indicator below the input.

---
"@jmfederico/pi-web": patch
---

Show workspace file contents and the file tree together in the resizable right sidebar instead of taking over the main chat area.

Allow opened workspace tool tabs, such as Info and Files, to be closed from the right sidebar header, folding the sidebar when the last tab closes.
